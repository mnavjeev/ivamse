% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vcov.R
\name{hatvalues.ivamse}
\alias{hatvalues.ivamse}
\title{Leverage Values for an \code{"ivamse"} Fit}
\usage{
\method{hatvalues}{ivamse}(model, ...)
}
\arguments{
\item{model}{an object of class \code{"ivamse"}.}

\item{...}{currently not used.}
}
\value{
A numeric vector with one value per observation. The values are
  between zero and one and sum to the number of coefficients.
}
\description{
Leverage of each observation, measuring how far its regressors
lie from the average. \code{\link[sandwich]{vcovHC}} needs these for its
\code{"HC2"} and \code{"HC3"} small-sample adjustments.
}
\examples{
set.seed(1)
N <- 300
z <- matrix(rnorm(N * 10), N, 10)
v <- rnorm(N)
x <- z[, 1] + v
y <- 2 * x + 0.7 * v + rnorm(N)
m <- ivamse_fit(y, x, z)

summary(hatvalues(m))
sum(hatvalues(m))

}
\seealso{
\code{\link{ivamse}}, \code{\link{vcovHC.ivamse}}.
}
